language: swift
osx_image: xcode9
cache: cocoapods

before_install:
  - gem install cocoapods

install:
  - pod install
script:
    - xcodebuild clean test -project FireBaseLoginRegister.xcodeproj -scheme FireBaseLoginRegister -destination "platform=iOS Simulator,name=iPhone 7,OS=10.3" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO -quiet
